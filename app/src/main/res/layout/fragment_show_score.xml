<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="MissingPrefix"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/colorWindow">

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:indeterminate="true"
        android:visibility="invisible"/>

    <TextView
        android:id="@+id/intro_message_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_gravity="center"
        android:textColor="@color/colorSecondaryText"
        android:text="@string/intro_message"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:layout_marginRight="@dimen/activity_horizontal_margin"
        android:visibility="invisible"
        android:textSize="22sp"
        fontPath="fonts/Roboto-Light.ttf"/>

    <lammar.com.csdemo.ui.controls.CreditScoreControl
        android:id="@+id/credit_score_control"
        android:layout_width="@dimen/credit_score_control_size"
        android:layout_height="@dimen/credit_score_control_size"
        android:background="@drawable/score_frame_bg"
        android:layout_gravity="center"
        android:visibility="invisible"/>
    <Button
        style="@style/PrimaryButton"
        android:theme="@style/PrimaryButton"
        android:id="@+id/get_score_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/get_my_score_button"
        android:visibility="invisible"
        android:layout_marginBottom="@dimen/activity_vertical_margin_large"
        android:layout_gravity="bottom|center_horizontal"/>

    <Button
        style="@style/SecondaryButton"
        android:theme="@style/SecondaryButton"
        android:id="@+id/start_again_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/start_again_button"
        android:visibility="invisible"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="@dimen/activity_vertical_margin_large"/>

</FrameLayout>